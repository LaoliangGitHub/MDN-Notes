页面默认的打开链接的地址：
有三个属性
href:链接的地址
如果指定了该属性，这个元素必须写在其他任何属性值是 URL 的元素之前。允许绝对和相对URL（但是请查看下面的注意节段）。

target属性：
_blank在新窗口中打开被链接文档。
_self默认（默认时不用写target）。在相同的框架中打开被链接文档。
_parent在父框架集中打开被链接文档。
_top在整个窗口中打开被链接文档。（all）

如果指定了多个 <base> 元素，只会使用第一个 href 和 target 值, 其余都会被忽略。